using ThermalClasses.GameObjects.Particles;
using Xunit;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
namespace Tests;

public class UnitTest1
{
    /*
    private Polygon NewSmallCircle(int identifier)
    {
        return new Polygon(content.Load<Texture2D>("SimulationAssets/YellowParticle"), new Vector2(20, 20), new Vector2(0, 0), 100, 50, Color.White, new Point(10, 10))
        {
            Enabled = false,
            Type = "Small",
            Identifier = identifier,
        };
    }
    */
    
    [Fact]
    public void SATCollisions()
    {

    }
}